spring:
  application:
    name: SpringBootRest
  datasource:
    url: jdbc:mysql://localhost:3306/develop
    username: root
    password: brigadier

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

